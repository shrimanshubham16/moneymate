{
  "name": "finflow-tests",
  "version": "1.0.0",
  "description": "Test suite for FinFlow application",
  "scripts": {
    "test:api": "node api/run-tests.js",
    "test:e2e": "npx playwright test",
    "test:all": "npm run test:api && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}

